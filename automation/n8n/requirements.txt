# Everything in automation/n8n/ is stdlib + this. httpx is optional at runtime (sarvam.py
# and alerts.py fall back to urllib.request when it's missing) but installed here so CI
# exercises the same code path the team runs locally.
httpx>=0.28,<1.0
pytest>=8.3,<9.0
