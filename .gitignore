# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Node
node_modules/
dist/
.vite/

# Env & secrets -- never commit real keys
.env
.env.local
*.pem
service-account*.json
gee-credentials*.json

# Data exports (keep data/samples/ and data/india/, ignore big raw pulls)
data/raw/
*.tif
*.zip

# Local download cache for pipelines/geo/registry.py (the GeoNames India dump) --
# 16 MB, re-fetched on demand, never something to commit.
backend/.cache/

# Editors / OS
.vscode/
.idea/
.DS_Store
